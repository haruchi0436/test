★sprintf→さまざまな書式で数字を文字列に変換することができます。

★substr 文字列の一部分を切り出す時に使います。
my $s = substr(元の文字列、切り出す開始位置、切り出す長さ);

       文字列の置き換え
my $s = substr(元の文字列、切り出す開始位置、切り出す長さ、切り出したところに入れる文字列);
*元の文字列の部分は変数でなければならない。

★index
文字列の中に含まれている別の文字列の位置を知ることができます

★reindex
一番最後に見つかった文字列の位置を返します。
